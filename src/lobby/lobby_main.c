/* ============================================================
 * @deps-implements: lobby server entry point (stub)
 * @deps-requires: stdio.h
 * @deps-last-changed: 2026-03-22 — Initial creation (Step 3: Multi-binary build)
 * ============================================================ */

#include <stdio.h>

int main(void)
{
    printf("hh-lobby starting...\n");
    return 0;
}
